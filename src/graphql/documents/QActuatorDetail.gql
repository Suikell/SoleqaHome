query QActuatorDetail($actuatorId: Int!) {
  actuator: actuatorNode(id: $actuatorId) {
    id
    currentState
    batteryLevel
    isOnline
    manualOverride
    thresholdGroupActuators {
      id
      priority
      thresholdGroup {
        id
        name
      }
    }
  }
}
